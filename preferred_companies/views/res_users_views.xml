<?xml version="1.0" encoding="utf-8" ?>
<odoo>
 <record id="res_users_simple_modif_view_form" model="ir.ui.view">
    <field name="name">res.users.simple.modif.view.form</field>
    <field name="model">res.users</field>
    <field name="inherit_id" ref="base.view_users_form_simple_modif" />
    <field name="arch" type="xml">
        <field name="signature" position="after">
            <field name="company_ids" invisible="1" />
            <group name="preferred_companies" string="Preferred companies">
                <field name="preferred_company_ids" 
                    widget="many2many_tags" 
                    options="{'no_create': True, 'no_open': True}" 
                    domain="[('id', 'in', company_ids)]"
                />
            </group>
        </field>
    </field>
 </record>
</odoo>